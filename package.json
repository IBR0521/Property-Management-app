{
  "name": "property-ops",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Property management operations app. Postgres (Supabase), deployed on Vercel.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node server/index.js",
    "seed": "node server/seed.js",
    "reset": "rm -rf data/app.db data/app.db-wal data/app.db-shm && npm run seed",
    "cron": "node -e \"import('./server/lib/scheduler.js').then(m=>m.tick('manual')).then(r=>console.log(r))\"",
    "test": "node --env-file=.env.test --test --test-concurrency=1 test/*.test.js",
    "test:watch": "node --env-file=.env.test --test --watch test/*.test.js",
    "icons": "node scripts/icons.js",
    "vapid": "node -e \"import('./server/lib/push/vapid.js').then(m=>{const k=m.generateVapidKeys();console.log('\\nVAPID_PUBLIC_KEY='+k.publicKey);console.log('VAPID_PRIVATE_KEY='+k.privateKey);console.log('VAPID_SUBJECT=mailto:you@example.com\\n');console.log('Put these in your environment. The public key is handed to browsers,');console.log('so changing it later invalidates every existing subscription.\\n')})\"",
    "db:test": "createdb propops_test 2>/dev/null; echo 'propops_test ready'",
    "check": "npm test && echo ALL_GREEN"
  },
  "dependencies": {
    "@vercel/blob": "^0.27.0",
    "pdf-lib": "^1.17.1",
    "postgres": "^3.4.0",
    "qrcode-generator": "^2.0.4"
  }
}
